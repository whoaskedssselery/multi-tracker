import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test placeholder', (WidgetTester tester) async {
    // Placeholder — full integration tests are out of scope for MVP.
    expect(true, isTrue);
  });
}
